apiVersion: v1
kind: Service
metadata:
  name: yardmaster-dashboard
  namespace: yardmaster-system
spec:
  selector:
    app.kubernetes.io/name: yardmaster-dashboard
  ports:
    - name: http
      port: 8088
      targetPort: http
